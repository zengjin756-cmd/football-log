# 绿茵日志 · 上云方案评估报告

> 版本：v1.0（2026-09-04）
> 状态：评估稿，供产品决策与团队评审
> 定位：从「单机高保真原型」演进为「真·多人协作产品」

---

## 0. 摘要（TL;DR）

| 项目 | 结论 |
|------|------|
| 推荐方案 | **方案 A：Supabase**（免费起，可自托管，不锁死） |
| 备选方案 | 方案 B：Firebase（海外向/移动App）／方案 C：腾讯云开发 CloudBase（主打微信小程序时反超） |
| 迁移成本 | **低**。前端继续静态托管（GitHub Pages 不动），只需替换"数据读写层" |
| 第一步 | 建 Supabase 免费项目 → 建数据表 + RLS 权限 → 前端数据层换 SDK → 跑通"真账号＋真好友"最小闭环 |
| 重大提醒 | ⚠️ LeanCloud 已于 2025 年底停服 —— 选型务必避开"会关停/绑死"的平台 |

---

## 1. 项目现状盘点

### 1.1 技术现状（已核验）
- **纯前端单文件**：`index.html`（约 92KB，HTML/CSS/JS 全内联），无 `package.json`、无构建工具、无框架依赖。
- **移动优先**：`<meta viewport>` 已配置，UI 按手机外壳（400px）设计。
- **存储**：浏览器本地存储，键分区：
  - `footballLog_data_<uid>` — 各账号数据分区（profile / trainings / matches / summaries / checkins / settings / notes）
  - `footballLog_rels_v` — 全局好友关系表 `{a,b,status:'friend'|'pending',at,lastPlayed?}`
  - `footballLog_cur_v` — 当前登录账号
  - `footballLog_accounts_v` — 账号目录
- **数据读写集中**：`readBox()` / `writeBox()` / `save()` / `load()` / `persistRels()` / `loginAs()` —— **这些是未来替换云端数据层的唯一改造点，范围很小。**

### 1.2 权限模型（现状＝模拟）
- 每个账号只有自己的数据分区（天然"只能改自己"）
- 好友关系表全局，通过 `areFriends(ME,u)` 决定"是否可见对方资料/动态"
- **这套模型与关系型数据库的"行级权限(RLS)"一一对应**，是上云迁移的巨大优势。

### 1.3 核心诉求（用户确认）
1. 真产品（非 Demo）
2. 数据不敏感（国内外云皆可，成本优先）
3. 要真·多人协作（好友见真数据、真多账号、跨设备同步）

---

## 2. 上云的本质：解决哪两个"硬伤"

现有单机原型的两个根本瓶颈，只有上云才能根治：

| 硬伤 | 单机现状 | 上云后 |
|------|---------|--------|
| 数据不跨端、易丢 | 存浏览器本地，清缓存即失 | 云端数据库，换设备数据还在 |
| 好友是"模拟"的 | 预置的演示账号关系 | 真实注册账号 + 真好友关系 |

> 结论：在这两个硬伤解决前，功能加再多也仍是"堆砌的 Demo"。上云是**从原型走向真产品的分水岭**。

---

## 3. 三条候选路线对比

### 3.1 方案 A：Supabase（推荐）
- **类型**：开源 BaaS（"开源版 Firebase"），基于原生 PostgreSQL。
- **能力**：PostgreSQL 关系库 + 现成登录 Auth + **行级权限 RLS** + 实时订阅 + 对象存储 + 自动生成 REST API。
- **免费额度**（2026 实测）：500MB 数据库、1GB 文件存储、**5 万月活认证**、每月 5GB 出站流量、2 个免费项目。
- **局限**：国内延迟 100–300ms；免费项目**连续 1 周无数据库活动会自动休眠**（唤醒约 30 秒，可免费用定时 ping 保活）。
- **订阅成本**：免费起 → `$25/月`（Pro，含 8GB 库，解除休眠限制）。
- **为什么最适合本项目**：
  - RLS 能精准实现"球员只能读写自己的记录、互为好友才可查对方公开资料"，与现有权限模型**逐条对应**。
  - 关系型库适合"训练/比赛/好友/打卡"这类**结构化、有关联**的数据。
  - 开源可自托管、数据可一键导出 → **不怕平台关停/被绑死**（直接回应 LeanCloud 教训）。
- **推荐指数**：⭐⭐⭐（本项目首选）

### 3.2 方案 B：Firebase（备选）
- **能力**：Firestore 文档库 + Auth（无限免费）+ 实时强 + 全平台 SDK。
- **免费额度**：1GB 库、5GB 存储、**无休眠**（比 Supabase 省心）、Auth 无限。
- **局限**：NoSQL 文档模型，**复杂关系/多表联查弱**；**国内访问受限**需过网络门槛；付费按读写量（低效查询易超支）。
- **订阅成本**：免费起 → 按量付费，5 千用户约 `$7/月`；Blaze 计划 `$0` 起 + 用量。
- **适用**：移动 App + 大量实时场景；纯 Web/关系型场景略重。
- **推荐指数**：⭐⭐（作为海外向/移动备选）

### 3.3 方案 C：腾讯云开发 CloudBase
- **能力**：腾讯国内节点、与微信生态强打通（登录/支付）、无需备案（小程序）、NoSQL + 云函数 + 云托管。
- **免费额度**：每个账号 1 个免费体验环境，每月 3000 点资源额度（需手动续期）。
- **局限**：免费额度紧（常驻容器易超额）；自定义域名需国内备案；**强绑定微信生态**，纯 Web/H5 场景反受限。
- **订阅成本**：按量计费。
- **适用**：**未来主打微信小程序**时此方案反超。
- **推荐指数**：⭐⭐（条件性——取决于主攻端）

---

## 4. 推荐决策

### 4.1 主推荐
> **先选 Supabase，用免费额度跑通真·多人最小闭环；若未来主打微信小程序，再评估迁 CloudBase。**

理由排序：
1. RLS 权限模型与现有业务**最契合**，迁移业务逻辑改动最小。
2. 关系型数据库更适合结构化运动数据。
3. 开源 + 可自托管 + 可导出 = 不被厂商锁定（避坑）。
4. 免费额度（5 万 MAU）足够支撑早期真实用户。

### 4.2 何时转向备选
- 确定主打**微信小程序**（含微信登录/支付）→ 优先 CloudBase。
- 确定主打**海外移动 App**、重实时协作 → 可考虑 Firebase。
- 都不是 → 维持 Supabase 即可。

---

## 5. 迁移路径与改造点

### 5.1 前端几乎不用动（优势）
- GitHub Pages 静态托管**保持不变**。
- 页面/交互/UI 全部不动。
- **唯一改造点**：数据读写层——把 `readBox/writeBox/save/load/persistRels/loginAs` 替换为 Supabase SDK 的查询/写入，并引入真实 Auth 登录态替代 `switchAccount` 模拟。

### 5.2 数据模型映射（单机 → 云端）
| 单机键 | 云端表 | 说明 |
|--------|--------|------|
| `footballLog_data_<uid>.profile` | `profiles` | 公开资料，RLS：本人可改，好友可读 |
| `.trainings` | `trainings` | RLS：owner 可读写 |
| `.matches` | `matches` | RLS：owner 可读写 |
| `.summaries` | `summaries` | RLS：owner 可读写 |
| `.checkins` | `checkins` | RLS：owner 可读写 |
| `.settings/.notes` | `settings` | 仅本人 |
| `footballLog_rels_v` | `friendships` | 关系表，RLS 双向可见 |

### 5.3 权限策略落点（RLS 规则示例）
- `trainings`：`auth.uid() = owner_id`（只能改自己）
- `profiles`：`auth.uid() = id`（改自己）`OR EXISTS(互为好友)`（好友读）
- `friendships`：`auth.uid() = a OR auth.uid() = b`（双向）

> 这套规则把现原型"只能编辑自己 / 加好友才可见"从**前端约定**升级为**数据库强制**，更安全、更真实。

### 5.4 登录体系演进
- 现状：`switchAccount()` 在预置演示账号间切换（模拟）。
- 上云后：Supabase Auth（支持邮箱/手机/OAuth），替代模拟账号；演示数据可用"一键生成示例账号"保留用于开发。

---

## 6. 分阶段落地计划

| 阶段 | 目标 | 交付物 | 预估投入 |
|------|------|--------|---------|
| **P0 基建** | 前端工程化 | 引入构建工具(vite)、拆分子模块、配置项抽离 | 0.5–1 天 |
| **P1 试连** | 建 Supabase 免费项目 + 数据表 + RLS | 建表 SQL + 权限策略；前端 SDK 接通 | 1–2 天 |
| **P2 最小闭环** | 真实注册→登录→录数据→加好友→看真数据 | 端到端跑通，替代模拟账号 | 2–3 天 |
| **P3 完善** | 跨端同步、真统计、错误态/加载态 | 稳定可用版本 | 3–5 天 |
| **P4 上线** | 备案/域名/监控/备份 | 生产就绪 | 视情况 |

> P1 起需你注册 Supabase（邮箱即可，免费）；我可协助生成建表 SQL 与前端改造脚本。

---

## 7. 风险与应对

| 风险 | 说明 | 应对 |
|------|------|------|
| 免费版休眠 | Supabase 免费项目 7 天无活动自动暂停 | 定时 ping 保活，或升级 Pro($25/月) |
| 国内访问延迟 | 100–300ms，海外节点 | 早期可用；规模化再评估国内云或自托管 |
| 厂商依赖 | 任何 BaaS 都有存续/价格风险 | **优先开源可自托管方案**；数据定期一键导出 |
| 数据迁移工作 | 有本地存量数据 | 提供 JSON 导入脚本一键上云 |
| 前期范围蔓延 | 一次做太多 | 严格按 P0→P4，先跑通闭环再扩展 |

---

## 8. 结论

**建议按"方案 A：Supabase"推进**，先落地 P1/P2（免费建库 → 最小闭环），用最小成本验证"真账号 + 真好友 + 跨端同步"是否成立；若后续转向微信小程序生态，再评估 CloudBase。前端保持静态托管，数据层是唯一改造点，整体迁移风险低、收益明确。

*——本报告基于 2026-09 调研与项目代码核验生成，方案内容可随产品方向调整。*
